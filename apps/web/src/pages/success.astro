---
import DefaultLayout from "@/layouts/DefaultLayout.astro";
---

<DefaultLayout>
  <div class="flex items-center flex-col w-full mt-12">
    <div class="bg-gradient-to-br from-green-50 to-emerald-50 border border-green-200 rounded-lg p-8 max-w-md w-full text-center shadow-sm">
      <div class="w-16 h-16 bg-green-100 border border-green-200 rounded-full flex items-center justify-center mx-auto mb-6">
        <svg class="w-8 h-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
        </svg>
      </div>
      
      <div class="prose prose-lg">
        <h1 class="text-green-800 mb-4">Pago Exitoso</h1>
        <p class="text-green-700 mb-2">Tu compra se ha procesado correctamente.</p>
        <p class="text-green-600 text-sm mb-6">Recibirás un email con los detalles de tu pedido.</p>
        
        <a 
          href="/" 
          class="btn btn-secondary text-stone-50 rounded-none no-underline hover:bg-opacity-90"
        >
          Volver al inicio
        </a>
      </div>
    </div>
  </div>
</DefaultLayout>

<script>
  import { useCartStore } from '@/stores/cart-store';
  
  const clearCart = useCartStore.getState().clearCart;
  clearCart();
</script>